# apps/backend/src/scripts/seed-superadmin.ts

Generated by `npm run docs:generate`. Do not edit this file.

[Source](../../../../../../apps/backend/src/scripts/seed-superadmin.ts) | [Guide](../../../../features/authentication.md) | [Guide](../../../../database.md) | [Guide](../../../../api.md) | [Guide](../../../../../design-decisions.md) | [Guide](../../../../features/superadmin-setup.md)

Source SHA-256: `0661895d39514bbde089932c77c9825a3b2392cfe458d8bbf7c5736b6a24516c`.

## Dependencies

- `reflect-metadata`
- `@nestjs/core`
- `@nestjs/config`
- `typeorm`
- `../app.module`
- `../modules/auth/auth.entities`
- `../modules/auth/seed-superadmin`

## Declarations and members

### main

````ts
async function main()
````

## Current source

````ts
import 'reflect-metadata';
import { NestFactory } from '@nestjs/core';
import { ConfigService } from '@nestjs/config';
import { DataSource } from 'typeorm';
import { AppModule } from '../app.module';
import { User } from '../modules/auth/auth.entities';
import { seedSuperadmin } from '../modules/auth/seed-superadmin';

async function main() {
  if (process.env.NODE_ENV === 'production')
    throw new Error(
      'The development superadmin seed is disabled in production.',
    );
  const app = await NestFactory.createApplicationContext(AppModule, {
    logger: ['error'],
  });
  try {
    const config = app.get(ConfigService);
    const result = await seedSuperadmin(
      app.get(DataSource).getRepository(User),
      {
        email: config.get<string>('SEED_SUPERADMIN_EMAIL', ''),
        password: config.get<string>('SEED_SUPERADMIN_PASSWORD', ''),
        nodeEnv: config.get<string>('NODE_ENV'),
      },
    );
    console.log(
      `Superadmin ${result.status}: ${result.user.email}. Passwords are never overwritten by this command.`,
    );
  } finally {
    await app.close();
  }
}
void main().catch((error) => {
  console.error((error as Error).message);
  process.exitCode = 1;
});
````
