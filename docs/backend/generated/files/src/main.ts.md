# apps/backend/src/main.ts

Generated by `npm run docs:generate`. Do not edit this file.

[Source](../../../../../apps/backend/src/main.ts) | [Guide](../../../runtime.md) | [Guide](../../../../getting-started.md) | [Guide](../../../../testing.md) | [Guide](../../../../feature-status.md)

Source SHA-256: `be54d7c6e7af604b2767edd20f0c2dad8f3605db111f7a54753691ec98bfd458`.

## Dependencies

- `@nestjs/config`
- `./modules/auth/auth-security.middleware`
- `@nestjs/common`
- `@nestjs/core`
- `@nestjs/swagger`
- `./app.module`

## Declarations and members

### bootstrap

````ts
async function bootstrap()
````

## Current source

````ts
import { ConfigService } from '@nestjs/config';
import { allowedOrigins } from './modules/auth/auth-security.middleware';
import { ValidationPipe } from '@nestjs/common';
import { NestFactory } from '@nestjs/core';
import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);

  app.enableCors({
    origin: allowedOrigins(app.get(ConfigService)),
    credentials: true,
  });

  app.setGlobalPrefix('api');
  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,
      transform: true,
    }),
  );

  const config = new DocumentBuilder()
    .setTitle('Shopping API')
    .setDescription('API for admin, vendor, and storefront workflows')
    .setVersion('1.0')
    .addTag('shopping')
    .build();

  const document = SwaggerModule.createDocument(app, config);
  SwaggerModule.setup('docs', app, document);

  const port = Number(process.env.PORT ?? 3000);
  await app.listen(port);
  console.log(`Shopping API listening on http://localhost:${port}`);
}

void bootstrap();
````
