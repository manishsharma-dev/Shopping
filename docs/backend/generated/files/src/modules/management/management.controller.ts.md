# apps/backend/src/modules/management/management.controller.ts

Generated by `npm run docs:generate`. Do not edit this file.

[Source](../../../../../../../apps/backend/src/modules/management/management.controller.ts) | [Guide](../../../../../features/management.md) | [Guide](../../../../../database.md) | [Guide](../../../../../api.md)

Source SHA-256: `e4de0923a93d96d47667b4f2420835517d0dbc42abc20f495d2b0b6e00ae5f24`.

## Dependencies

- `@nestjs/common`
- `../auth/auth.guard`
- `../auth/auth.guard`
- `./management.dto`
- `./management.service`

## Declarations and members

### ManagementController

`@Controller(...)` `@UseGuards(...)`

| Member | Declaration, decorators, or initializer |
| --- | --- |
| constructor | constructor(private readonly service: ManagementService) |
| applications | @Get('applications') applications(@Req() req: AuthRequest) |
| snapshot | @Get() snapshot(     @Req() req: AuthRequest,     @Query('state') state?: string,     @Query('district') district?: string,     @Query('vendorId') vendorId?: string,   ) |
| apply | @Post('apply') apply(@Req() req: AuthRequest, @Body() dto: ManagementDto) |
| user | @Post('users') user(@Req() req: AuthRequest, @Body() dto: ManagementDto) |
| userStatus | @Post('users/:id/status') userStatus(     @Req() req: AuthRequest,     @Param('id') id: string,     @Body() dto: ManagementDto,   ) |
| userType | @Post('users/:id/type') userType(     @Req() req: AuthRequest,     @Param('id') id: string,     @Body() dto: ManagementDto,   ) |
| create | @Post(':kind') create(     @Req() req: AuthRequest,     @Param('kind') kind: string,     @Body() dto: ManagementDto,   ) |
| update | @Post(':kind/:id') update(     @Req() req: AuthRequest,     @Param('kind') kind: string,     @Param('id') id: string,     @Body() dto: ManagementDto,   ) |

## Current source

````ts
import {
  Body,
  Controller,
  Get,
  Param,
  Post,
  Query,
  Req,
  UseGuards,
} from '@nestjs/common';
import { AuthGuard } from '../auth/auth.guard';
import type { AuthRequest } from '../auth/auth.guard';
import { ManagementDto } from './management.dto';
import { ManagementService } from './management.service';

@Controller('manage')
@UseGuards(AuthGuard)
export class ManagementController {
  constructor(private readonly service: ManagementService) {}
  @Get('applications') applications(@Req() req: AuthRequest) {
    return this.service.applications(req.user);
  }
  @Get() snapshot(
    @Req() req: AuthRequest,
    @Query('state') state?: string,
    @Query('district') district?: string,
    @Query('vendorId') vendorId?: string,
  ) {
    return this.service.snapshot(req.user, { state, district, vendorId });
  }
  @Post('apply') apply(@Req() req: AuthRequest, @Body() dto: ManagementDto) {
    return this.service.apply(req.user, dto.data);
  }
  @Post('users') user(@Req() req: AuthRequest, @Body() dto: ManagementDto) {
    return this.service.createUser(req.user, dto.data);
  }
  @Post('users/:id/status') userStatus(
    @Req() req: AuthRequest,
    @Param('id') id: string,
    @Body() dto: ManagementDto,
  ) {
    return this.service.userStatus(req.user, id, dto.data);
  }
  @Post('users/:id/type') userType(
    @Req() req: AuthRequest,
    @Param('id') id: string,
    @Body() dto: ManagementDto,
  ) {
    return this.service.assignType(req.user, id, dto.data);
  }
  @Post(':kind') create(
    @Req() req: AuthRequest,
    @Param('kind') kind: string,
    @Body() dto: ManagementDto,
  ) {
    return this.service.save(req.user, kind, dto);
  }
  @Post(':kind/:id') update(
    @Req() req: AuthRequest,
    @Param('kind') kind: string,
    @Param('id') id: string,
    @Body() dto: ManagementDto,
  ) {
    return this.service.save(req.user, kind, dto, id);
  }
}
````
