# apps/backend/src/modules/auth/auth.guard.ts

Generated by `npm run docs:generate`. Do not edit this file.

[Source](../../../../../../../apps/backend/src/modules/auth/auth.guard.ts) | [Guide](../../../../../features/authentication.md) | [Guide](../../../../../database.md) | [Guide](../../../../../api.md) | [Guide](../../../../../../design-decisions.md) | [Guide](../../../../../features/superadmin-setup.md)

Source SHA-256: `892ae592497b1fea96d708d9efd0f83f0120f6bc1da1732b9d4689a6d958b165`.

## Dependencies

- `@nestjs/common`
- `express`
- `./auth.service`

## Declarations and members

### AuthRequest

````ts
export type AuthRequest = Request & { user: ReturnType<typeof publicUser> };
````

### sessionToken

````ts
export function sessionToken(request: Request): string
````

### AuthGuard

`@Injectable(...)`

Inheritance: `implements CanActivate`

| Member | Declaration, decorators, or initializer |
| --- | --- |
| constructor | constructor(private readonly auth: AuthService) |
| canActivate | async canActivate(context: ExecutionContext) |

### AdminGuard

`@Injectable(...)`

Inheritance: `implements CanActivate`

| Member | Declaration, decorators, or initializer |
| --- | --- |
| canActivate | canActivate(context: ExecutionContext) |

## Current source

````ts
import {
  CanActivate,
  ExecutionContext,
  ForbiddenException,
  Injectable,
} from '@nestjs/common';
import type { Request } from 'express';
import { AuthService, publicUser } from './auth.service';

export type AuthRequest = Request & { user: ReturnType<typeof publicUser> };
export function sessionToken(request: Request): string {
  return (
    request.headers.cookie
      ?.split(';')
      .map((part) => part.trim())
      .find((part) => part.startsWith('shopping_session='))
      ?.slice('shopping_session='.length) ?? ''
  );
}

@Injectable()
export class AuthGuard implements CanActivate {
  constructor(private readonly auth: AuthService) {}
  async canActivate(context: ExecutionContext) {
    const request = context.switchToHttp().getRequest<AuthRequest>();
    request.user = await this.auth.authenticate(sessionToken(request));
    return true;
  }
}

@Injectable()
export class AdminGuard implements CanActivate {
  canActivate(context: ExecutionContext) {
    const { user } = context.switchToHttp().getRequest<AuthRequest>();
    if (!user || !['admin', 'superadmin'].includes(user.role))
      throw new ForbiddenException('Administrator access required');
    return true;
  }
}
````
