# apps/backend/src/modules/auth/auth.dto.ts

Generated by `npm run docs:generate`. Do not edit this file.

[Source](../../../../../../../apps/backend/src/modules/auth/auth.dto.ts) | [Guide](../../../../../features/authentication.md) | [Guide](../../../../../database.md) | [Guide](../../../../../api.md) | [Guide](../../../../../../design-decisions.md) | [Guide](../../../../../features/superadmin-setup.md)

Source SHA-256: `cc09f0941f5463ba11cef31af5e67d3337109955d7df268a7eada07b1f3a2fe8`.

## Dependencies

- `class-transformer`
- `class-validator`

## Declarations and members

### LoginDto

Class declaration.

| Member | Declaration, decorators, or initializer |
| --- | --- |
| email | @Transform(({ value }: { value: unknown }) =&gt;     typeof value === 'string' ? value.trim().toLowerCase() : value,   )   @IsEmail()   @MaxLength(254)   email: string; |
| password | @IsString()   @MinLength(1)   @MaxLength(128)   password: string; |

### RegisterDto

Class declaration.

Inheritance: `extends LoginDto`

| Member | Declaration, decorators, or initializer |
| --- | --- |
| name | @Transform(({ value }: { value: unknown }) =&gt;     typeof value === 'string' ? value.trim() : value,   )   @IsString()   @MinLength(1)   @MaxLength(100)   name: string; |
| password | @MinLength(12)   declare password: string; |

## Current source

````ts
import { Transform } from 'class-transformer';
import { IsEmail, IsString, MaxLength, MinLength } from 'class-validator';

export class LoginDto {
  @Transform(({ value }: { value: unknown }) =>
    typeof value === 'string' ? value.trim().toLowerCase() : value,
  )
  @IsEmail()
  @MaxLength(254)
  email: string;

  @IsString()
  @MinLength(1)
  @MaxLength(128)
  password: string;
}

export class RegisterDto extends LoginDto {
  @Transform(({ value }: { value: unknown }) =>
    typeof value === 'string' ? value.trim() : value,
  )
  @IsString()
  @MinLength(1)
  @MaxLength(100)
  name: string;

  @MinLength(12)
  declare password: string;
}
````
