# apps/admin/src/app/core/auth/auth.guard.ts

Generated by `npm run docs:generate`. Do not edit this file.

[Source](../../../../../../../../../apps/admin/src/app/core/auth/auth.guard.ts) | [Guide](../../../../../../../authentication.md) | [Guide](../../../../../../../admin.md) | [Guide](../../../../../../../../design-decisions.md)

Source SHA-256: `186c2eaebc6b570e0b0d05d7340417e3d0a84a53bbf257c00d9aefa0f3a9ac65`.

## Dependencies

- `@angular/core`
- `@angular/router`
- `./auth.service`

## Declarations and members

### adminGuard

````ts
export const adminGuard: CanActivateFn = async () => {
  const auth = inject(AuthService);
  const router = inject(Router);
  try {
    await auth.restore();
  } catch {
    return router.createUrlTree(['/login']);
  }
  return (
    ['admin', 'superadmin'].includes(auth.user()?.role ?? '') || router.createUrlTree(['/login'])
  );
};
````

## Current source

````ts
import { inject } from '@angular/core';
import { CanActivateFn, Router } from '@angular/router';
import { AuthService } from './auth.service';

export const adminGuard: CanActivateFn = async () => {
  const auth = inject(AuthService);
  const router = inject(Router);
  try {
    await auth.restore();
  } catch {
    return router.createUrlTree(['/login']);
  }
  return (
    ['admin', 'superadmin'].includes(auth.user()?.role ?? '') || router.createUrlTree(['/login'])
  );
};
````
