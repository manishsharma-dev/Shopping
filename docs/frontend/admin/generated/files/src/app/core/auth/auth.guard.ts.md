# apps/admin/src/app/core/auth/auth.guard.ts

Generated by `npm run docs:generate`. Do not edit this file.

[Source](../../../../../../../../../apps/admin/src/app/core/auth/auth.guard.ts) | [Guide](../../../../../../../authentication.md) | [Guide](../../../../../../../admin.md) | [Guide](../../../../../../../../design-decisions.md)

Source SHA-256: `828515370b779419c5f1456b8df670a6bb35dc359bcba7d468fd7cfec1505e60`.

## Dependencies

- `@angular/core`
- `@angular/router`
- `./auth.service`
- `./access.service`

## Declarations and members

### adminGuard

````ts
export const adminGuard: CanActivateFn = async (route) => {
  const auth = inject(AuthService);
  const access = inject(AccessService);
  const router = inject(Router);
  try {
    await auth.restore();
    if (!auth.user() || auth.user()?.role === 'customer') return router.createUrlTree(['/login']);
    await access.load();
    return (
      access.canVisit(route.data['section'] || 'dashboard') || router.createUrlTree(['/dashboard'])
    );
  } catch {
    return router.createUrlTree(['/login']);
  }
};
````

## Current source

````ts
import { inject } from '@angular/core';
import { CanActivateFn, Router } from '@angular/router';
import { AuthService } from './auth.service';
import { AccessService } from './access.service';
export const adminGuard: CanActivateFn = async (route) => {
  const auth = inject(AuthService);
  const access = inject(AccessService);
  const router = inject(Router);
  try {
    await auth.restore();
    if (!auth.user() || auth.user()?.role === 'customer') return router.createUrlTree(['/login']);
    await access.load();
    return (
      access.canVisit(route.data['section'] || 'dashboard') || router.createUrlTree(['/dashboard'])
    );
  } catch {
    return router.createUrlTree(['/login']);
  }
};
````
