# apps/admin/src/app/app.html

Generated by `npm run docs:generate`. Do not edit this file.

[Source](../../../../../../../apps/admin/src/app/app.html) | [Guide](../../../../../admin.md) | [Guide](../../../../../design.md) | [Guide](../../../../../../feature-status.md) | [Guide](../../../../../../testing.md)

Source SHA-256: `02fd1dfa0c9e4a377df32a89215a2eec00b2c71895f7969da880c5f409743ace`.


## Current source

````html
<mat-sidenav-container
  class="app-shell"
  [class.authenticated]="isWorkspace()"
  [hasBackdrop]="compact()"
  (backdropClick)="setSidebarOpen(false)"
>
  <mat-sidenav
    #drawer
    id="admin-sidebar"
    class="sidebar"
    [mode]="compact() ? 'over' : 'side'"
    [opened]="sidebarOpen()"
    [autoFocus]="compact()"
    (keydown.escape)="setSidebarOpen(false)"
    [attr.role]="compact() ? 'dialog' : 'complementary'"
    [attr.aria-modal]="compact() && sidebarOpen() ? 'true' : null"
    aria-label="Administration sidebar"
  >
    @if (isWorkspace()) {
      <aside class="sidebar-content">
        <div class="sidebar-heading">
          <a class="wordmark" routerLink="/dashboard" (click)="closeMobileSidebar()"
            ><span class="brand-symbol" aria-hidden="true">N</span
            ><span>Northstar<small>Administration</small></span></a
          >
          <button
            mat-icon-button
            type="button"
            aria-label="Close sidebar"
            (click)="setSidebarOpen(false)"
          >
            <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m14 6-6 6 6 6" /></svg>
          </button>
        </div>
        <nav class="nav" aria-label="Administration">
          @for (item of access.menu(); track item.path) {
            <a
              [routerLink]="item.path"
              routerLinkActive="active"
              ariaCurrentWhenActive="page"
              (click)="closeMobileSidebar()"
              >{{ item.label }}</a
            >
          }
        </nav>
        <div class="profile-card">
          <strong>{{ session.user()?.name }}</strong
          ><span>{{ session.user()?.userType || session.user()?.role }}</span>
          <button mat-stroked-button (click)="logout()">Sign out</button>
          @if (error()) {
            <p role="alert">{{ error() }}</p>
          }
        </div>
      </aside>
    }
  </mat-sidenav>
  <mat-sidenav-content>
    <main class="content">
      @if (isWorkspace()) {
        <header class="workspace-header">
          <div class="workspace-start">
            <button
              mat-icon-button
              type="button"
              class="sidebar-toggle"
              [attr.aria-label]="sidebarOpen() ? 'Close sidebar' : 'Open sidebar'"
              aria-controls="admin-sidebar"
              [attr.aria-expanded]="sidebarOpen()"
              (click)="setSidebarOpen(!sidebarOpen())"
            >
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 6h16M4 12h16M4 18h16" /></svg>
            </button>
            <div class="workspace-label"><strong>Workspace</strong><span>Overview</span></div>
          </div>
          <app-theme-toggle />
        </header>
      }
      <div class="page-content"><router-outlet /></div>
    </main>
  </mat-sidenav-content>
</mat-sidenav-container>
````
