# apps/admin/src/app/pages/login/login.component.ts

Generated by `npm run docs:generate`. Do not edit this file.

[Source](../../../../../../../../../apps/admin/src/app/pages/login/login.component.ts) | [Guide](../../../../../../../authentication.md) | [Guide](../../../../../../../admin.md) | [Guide](../../../../../../../../design-decisions.md)

Source SHA-256: `0b1210058c4c07dfa7bd0d71522a22ebbed4ebe8151b72c41540fbf1592beb1d`.

## Dependencies

- `@angular/core`
- `@angular/forms`
- `@angular/router`
- `@angular/material/button`
- `@angular/material/form-field`
- `@angular/material/input`
- `../../core/auth/auth.service`
- `../../core/theme/theme-toggle.component`

## Declarations and members

### LoginPage

`@Component(...)`

| Member | Declaration, decorators, or initializer |
| --- | --- |
| auth | private readonly auth = inject(AuthService); |
| router | private readonly router = inject(Router); |
| email | email = ''; |
| password | password = ''; |
| busy | readonly busy = signal(false); |
| error | readonly error = signal(''); |
| hidePassword | readonly hidePassword = signal(true); |
| submit | async submit() |

## Current source

````ts
import { Component, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { Router } from '@angular/router';
import { MatButtonModule } from '@angular/material/button';
import { MatFormFieldModule } from '@angular/material/form-field';
import { MatInputModule } from '@angular/material/input';
import { AuthService } from '../../core/auth/auth.service';
import { ThemeToggle } from '../../core/theme/theme-toggle.component';

@Component({
  standalone: true,
  imports: [FormsModule, MatButtonModule, MatFormFieldModule, MatInputModule, ThemeToggle],
  templateUrl: './login.component.html',
})
export class LoginPage {
  private readonly auth = inject(AuthService);
  private readonly router = inject(Router);
  email = '';
  password = '';
  readonly busy = signal(false);
  readonly error = signal('');
  readonly hidePassword = signal(true);
  async submit() {
    if (this.busy()) return;
    this.busy.set(true);
    this.error.set('');
    try {
      await this.auth.login(this.email, this.password);
      this.password = '';
      if (!['admin', 'superadmin'].includes(this.auth.user()!.role)) {
        await this.auth.logout();
        throw new Error('This account does not have administrator access.');
      }
      await this.router.navigateByUrl('/dashboard');
    } catch (e) {
      this.error.set((e as Error).message);
    } finally {
      this.busy.set(false);
    }
  }
}
````
