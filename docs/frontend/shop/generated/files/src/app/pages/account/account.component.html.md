# apps/shop/src/app/pages/account/account.component.html

Generated by `npm run docs:generate`. Do not edit this file.

[Source](../../../../../../../../../apps/shop/src/app/pages/account/account.component.html) | [Guide](../../../../../../../authentication.md) | [Guide](../../../../../../../shop.md) | [Guide](../../../../../../../../design-decisions.md)

Source SHA-256: `e8b3be2dbed432d188395b411adae53e3ded233173f2606525233aec2b0f4b91`.


## Current source

````html
<section class="auth-page account-page">
  <div class="auth-frame">
    <aside class="auth-story">
      <div>
        <p class="eyebrow">Welcome to Northstar</p>
        <h2>Good finds.<br />Great beginnings.</h2>
        <p class="story-description">
          A space for your everyday favourites. Sign in and make yourself at home.
        </p>
      </div>
      <svg class="story-art" viewBox="0 0 360 150" fill="none" aria-hidden="true">
        <circle cx="180" cy="76" r="69" fill="currentColor" opacity=".08" />
        <rect x="128" y="46" width="104" height="90" rx="16" fill="currentColor" opacity=".25" />
        <path
          d="M154 52V35a26 26 0 0 1 52 0v17"
          stroke="currentColor"
          stroke-width="7"
          stroke-linecap="round"
        />
        <path
          d="m163 91 12 12 24-28"
          stroke="currentColor"
          stroke-width="7"
          stroke-linecap="round"
          stroke-linejoin="round"
        />
        <circle cx="86" cy="58" r="11" fill="currentColor" opacity=".35" />
        <path
          d="M274 90v24m-12-12h24"
          stroke="currentColor"
          stroke-width="5"
          stroke-linecap="round"
          opacity=".6"
        />
      </svg>
      <div class="story-footnote">
        <span class="status-dot"></span>Your next favourite starts here.
      </div>
    </aside>
    <div class="auth-form-panel">
      @if (loading()) {
        <div class="account-loading" role="status">
          <mat-spinner diameter="32" /><span>Loading your account...</span>
        </div>
      } @else if (auth.user(); as user) {
        <div class="account-avatar" aria-hidden="true">
          {{ user.name.slice(0, 1).toUpperCase() }}
        </div>
        <p class="eyebrow">Your account</p>
        <h1>Hello, {{ user.name }}.</h1>
        <p class="auth-subtitle">You're signed in as {{ user.email }}.</p>
        <button mat-stroked-button (click)="logout()" [disabled]="busy()">
          {{ busy() ? 'Signing out...' : 'Sign out' }}
        </button>
      } @else {
        <p class="eyebrow">{{ registering() ? 'Join the community' : 'Your Northstar account' }}</p>
        <h1>{{ registering() ? 'Make it yours.' : 'Welcome back.' }}</h1>
        <p class="auth-subtitle">
          {{
            registering()
              ? 'Create an account to get started.'
              : 'Sign in to your little corner of Northstar.'
          }}
        </p>
        <form
          class="auth-form"
          (ngSubmit)="form.valid && submit()"
          #form="ngForm"
          [attr.aria-busy]="busy()"
        >
          @if (registering()) {
            <mat-form-field appearance="outline"
              ><mat-label>Full name</mat-label>
              <input
                matInput
                name="name"
                [(ngModel)]="name"
                required
                maxlength="100"
                autocomplete="name"
              />
              <mat-error>Enter your name.</mat-error>
            </mat-form-field>
          }
          <mat-form-field appearance="outline"
            ><mat-label>Email address</mat-label>
            <input
              matInput
              type="email"
              name="email"
              [(ngModel)]="email"
              required
              email
              maxlength="254"
              autocomplete="username"
            />
            <mat-error>Enter a valid email address.</mat-error>
          </mat-form-field>
          <mat-form-field appearance="outline"
            ><mat-label>Password</mat-label>
            <input
              matInput
              name="password"
              [type]="hidePassword() ? 'password' : 'text'"
              [(ngModel)]="password"
              required
              [minlength]="registering() ? 12 : 1"
              maxlength="128"
              [autocomplete]="registering() ? 'new-password' : 'current-password'"
            />
            <button
              mat-button
              matSuffix
              type="button"
              (click)="hidePassword.set(!hidePassword())"
              [attr.aria-label]="hidePassword() ? 'Show password' : 'Hide password'"
            >
              {{ hidePassword() ? 'Show' : 'Hide' }}
            </button>
            @if (registering()) {
              <mat-hint>Use at least 12 characters.</mat-hint>
            }
            <mat-error>{{
              registering() ? 'Use at least 12 characters.' : 'Enter your password.'
            }}</mat-error>
          </mat-form-field>
          <button
            mat-flat-button
            class="auth-submit"
            type="submit"
            [disabled]="form.invalid || busy()"
          >
            {{ busy() ? 'Please wait...' : registering() ? 'Create account' : 'Sign in' }}
          </button>
        </form>
        <button
          mat-button
          class="auth-switch"
          type="button"
          (click)="switchMode()"
          [disabled]="busy()"
        >
          {{ registering() ? 'Already have an account? Sign in' : 'New here? Create an account' }}
        </button>
      }
      @if (error()) {
        <p role="alert" class="auth-error">{{ error() }}</p>
      }
    </div>
  </div>
  <footer class="auth-footer">Northstar Market &middot; Everyday, thoughtfully chosen.</footer>
</section>
````
