# district-import.sql

Generated by `npm run docs:generate`. Do not edit this file.

[Source](../../../district-import.sql) | [Guide](../../backend/features/demographics.md) | [Guide](../../backend/database.md)

Source SHA-256: `dba8b9dc7febf6f448857ed2c8bef3f4fee365dd297efcd855bfbc18aff0c7b8`.


## Current source

````sql
\set ON_ERROR_STOP on
BEGIN;
CREATE TEMP TABLE district_import (state_name text NOT NULL, name varchar(100) NOT NULL, UNIQUE(state_name,name)) ON COMMIT DROP;
INSERT INTO district_import(state_name,name) VALUES
-- Dataset omitted here; follow the source link for all district pairs.
;
SELECT DISTINCT i.state_name AS unmatched_state FROM district_import i LEFT JOIN public.states s ON s.name=i.state_name WHERE s.id IS NULL;
DO $$
BEGIN
 IF EXISTS (SELECT 1 FROM district_import i LEFT JOIN public.states s ON s.name=i.state_name WHERE s.id IS NULL) THEN
  RAISE EXCEPTION 'Source contains unmatched states';
 END IF;
END $$;
WITH inserted AS (
 INSERT INTO public.districts(state_id,name)
 SELECT s.id,i.name FROM district_import i JOIN public.states s ON s.name=i.state_name
 ON CONFLICT (state_id,name) DO NOTHING
 RETURNING id
) SELECT count(*) AS inserted_districts FROM inserted;
DO $$
BEGIN
 IF EXISTS (SELECT 1 FROM district_import i JOIN public.states s ON s.name=i.state_name LEFT JOIN public.districts d ON d.state_id=s.id AND d.name=i.name WHERE d.id IS NULL) THEN
  RAISE EXCEPTION 'Import verification failed';
 END IF;
END $$;
COMMIT;
SELECT count(*) AS total_districts, count(DISTINCT state_id) AS states_covered FROM public.districts;
SELECT count(*) AS orphan_districts FROM public.districts d LEFT JOIN public.states s ON s.id=d.state_id WHERE s.id IS NULL;
````
